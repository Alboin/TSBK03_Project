Textur - ändra textur/färg beroende på normal? Görs enklast i shader. Kan nog lägga till något slags noise här också?

Ändra funktionen för att skapa ett nytt voxel-grid så att den tar in bara en dimension (som sätts till alla sen) och en cell-size, för att bestämma storleken på en cell, eller storleken på hela grid:et.

openmp till triangel-generering, mät tid före och efter implementation.

1. Skapa disjoint mesh, alla positioner är i int (x,y,z)

gör en map[x][y][z] = polygon för varje vertex

skapa en vector<ivec3> och initiera alla till ivec3(-1,-1,-1) (resize(#vertices / 3))

loopa igenom map:en, för varje element:
	lägg till vertex:en i vertex-vectorn
	lägg till indexet som den fick i vertex-vectorn i index-vectorn på en plats i 		vec3 där det är -1.

PASS 1
Skapa vertices utan att skala om allt. För varje vertex, lägg till dess triangel-index i map:en vertexTriangle[x][y][z] = pair<id1, id2>

PASS 2
Gå igenom alla värden i vertexTriangle och där samma vertex (egentligen samma vertex-pos) används till två trianglar, lägg till id:t på den vertex som ska tas bort i en lista, och byt sedan så att båda trianglarna innehåller samma vertex med samma id. 

PASS 3 (kanske kan skippas? kommer ju ta extra minne men kanske är värt det istället för en massa extra beräkningar?)
Gå igenom listan med vertices som skall tas bort, när en vertex tas bort,  

PASS 4
Gå igenom alla vertices och skala om till rätt position.



varje voxel får ett id = x + y * _dim + z * _dim * _dim

varje vertex förknippas med ett voxel-id




